{{>images}}
<div class="signers container">
    <br>

    <div>
        <br>
        {{#data}}
        <p class="signers">
            {{#if url}} <a href="http://{{url}}"> {{/if}} {{first}} {{last}} {{#if url}}</a>{{/if}} &ensp; {{#if age}} <span class="age"> {{age}}</span> {{/if}} &ensp; {{#if city}} city:&ensp;<a href="signers/{{city}}"> {{city}}</a>{{/if}}</p><br>
        {{/data}}

    {{#if result}}
        <br>
        {{#result}}
        {{#if url}} <a href="http://{{url}}"> {{/if}} {{first}} {{last}} {{#if url}}</a>{{/if}} &ensp; {{#if age}} <span class="age"> {{age}}</span> {{/if}} &ensp;</p><br>
        {{/result}}
    {{/if}}

    </div>
</div>
{{>logout}}
