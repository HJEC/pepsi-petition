<div class="thanks container">
    <h1>Thank you for signing the Petition</h1>
    {{#if sig}}
    <img src="{{sig}}"><br>
        {{else}}
    <img src="/dogs.jpg" alt="funny meme thing" class="jpeg">
    {{/if}}

    <form method="post">
    <p>click here to see others who signed</p>
    <button type="submit" name="booty">Click Me!</button>
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    </form>

{{#if data}}
<div class="results">
<ul>
    <br>
    {{#data}}
    <li>Name: {{this.first}} {{this.last}} time: {{this.t_stamp}} signature:</li>
    <img src="{{this.signature}}"><br>
    {{/data}}

    </ul>
</div>
{{/if}}
</div>
